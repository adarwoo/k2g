[package]
name = "xtask"
version = "0.1.0"
license = "GPL-3.0-only"
edition = "2021"
# Internal to k2g, never published to crates.io. Stated rather than assumed:
# it is what lets cargo-deny accept the workspace's path dependencies.
publish = false

[dependencies]
anyhow = "1"
serde_yaml = "0.9"
serde_json = "1"
regex = "1"
walkdir = "2"
